int removeElement(int* nums, int n, int val) {
    int l = 0;  
    for (int i = 0; i < n; i++) {
        if (nums[i] != val) {
            nums[l++] = nums[i];  
        }
    }
    
    return l;  
}
